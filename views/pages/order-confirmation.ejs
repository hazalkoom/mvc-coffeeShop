<%- include('../partials/header') %>

<section class="py-5">
    <div class="container text-center">
        <i class="fas fa-check-circle fa-3x text-success mb-3"></i>
        <h2>Thank you! Your order has been placed.</h2>
        <p class="text-muted">Order #<%= orderId %></p>
        <p class="lead">Total: $<%= Number(total).toFixed(2) %></p>
        <div class="mt-4">
            <a href="/orders" class="btn btn-primary"><i class="fas fa-history me-2"></i>View Orders</a>
            <a href="/products" class="btn btn-outline-secondary ms-2"><i class="fas fa-store me-2"></i>Continue Shopping</a>
        </div>
    </div>
</section>

<%- include('../partials/footer') %>


